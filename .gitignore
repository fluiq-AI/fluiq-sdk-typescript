# Dependencies
node_modules

# Lockfile is regenerated by `npm install` in CI, not committed
package-lock.json

# Build output (published to npm, not committed)
dist

# Secrets / local env
.env
tests/.env

# Tests are kept local (not part of the published repo)
tests

# Local vectorstore data / scratch
.chroma

# npm pack output & debug logs
*.tgz
npm-debug.log*
